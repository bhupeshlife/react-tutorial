function Codepoint(){
    const codepoint = "Hey Hello"; // Example codepoint for a grinning face emoji
    return (
        <h1>Hi Brat {codepoint}</h1> // {codepoint} is a evaluation expression that is evaluated and converted to a string 
    )
}

export default Codepoint;